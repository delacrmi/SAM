<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

   <item android:id="@+id/connect"
       android:title="@string/unplugged"
        android:icon="@drawable/unplugged"
        android:orderInCategory="100"
        app:showAsAction="always" />

    <!-- <item android:id="@+id/action_buscar"
        android:title="@string/accion_buscar"
        android:icon="@drawable/search"
        android:orderInCategory="100"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_nuevo"
        android:title="@string/accion_nuevo"
        android:icon="@drawable/add"
        android:orderInCategory="100"
        app:showAsAction="ifRoom|withText" /> -->
</menu>
